# ── IA / Gemini ──────────────────────────────────────────
google-generativeai>=0.8.0       # SDK oficial de Gemini API

# ── WordPress Integration ────────────────────────────────
requests>=2.31.0                 # Llamadas a WordPress REST API

# ── Interfaz Web ─────────────────────────────────────────
flask>=3.0.0                     # Framework web (reemplaza Streamlit)

# ── Scraping de Amazon (para parsear URLs) ───────────────
beautifulsoup4>=4.12.0           # Parsear HTML de páginas de producto
httpx>=0.27.0                    # Cliente HTTP (scraping de Amazon)
dnspython>=2.6.0                 # Resolver DNS alternativo (bypass bloqueo DNS corporativo)
truststore>=0.9.1                # Usar almacén de certs del SO (para redes con proxy TLS)
Pillow>=10.0.0                   # Redimensionar imágenes antes de subir a WordPress

# ── Utilidades ───────────────────────────────────────────
python-dotenv>=1.0.0             # Cargar variables de entorno desde .env
pydantic>=2.0.0                  # Validación de datos y modelos
tenacity>=8.2.0                  # Retry automático para llamadas a API
loguru>=0.7.0                    # Logging estructurado
